
allprojects {
    group = 'jmp'
    version = '1.0.0-SNAPSHOT'
}

subprojects {
    apply plugin: 'java'

    repositories {
        jcenter()
    }
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.9'
}